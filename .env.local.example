# K-MIRROR AI — Environment Variables
# Copy this file to .env.local and fill in your values.
#
# Get your Gemini API key from: https://aistudio.google.com/apikey
#
# How it works:
#   .env.local (GEMINI_API_KEY)
#       ↓  vite.config.ts loadEnv()
#   process.env.API_KEY  (used by services/geminiService.ts line 12)
#   process.env.GEMINI_API_KEY  (alias, same value)
#
# Only one variable needed here. Vite maps it to both names at build time.

GEMINI_API_KEY=your_gemini_api_key_here
